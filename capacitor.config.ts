import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.onestopdelivery.rider",
  appName: "OnestopRider",
  webDir: "www",
  server: {
    androidScheme: "https",
  },
};

export default config;
